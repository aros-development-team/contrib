# POV-Ray

This directory contains the AROS port of the POV-Ray ray tracer.

The contrib build produces the `povray` renderer from the included source tree and AROS-specific platform code, with PNG support.
