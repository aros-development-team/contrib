# m68kemul

`m68kemul` is a Motorola 68000 emulator included in AROS contrib.

It implements M68k instructions, addressing modes and condition codes and can run an input file with optional CPU-state dumping or single-step execution.
