# text2pdf

text2pdf 1.1 converts plain text to PDF output.

It supports configurable fonts, page layout and columns, form-feed handling and optional ISO Latin-1 encoding. The generated output uses PDF 1.1.
