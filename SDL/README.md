# SDL 1.2

This directory contains the SDL 1.2 port for AROS together with companion SDL libraries.

`SDL_main` provides the core SDL port. The remaining directories contain add-ons such as SDL_gfx, SDL_image, SDL_mixer, SDL_net, SDL_sound and SDL_ttf, using either fetched upstream sources or source included in the tree.
