# NewVox

NewVox is a landscape-rendering demo that illustrates the basic idea behind a voxel-style rendering technique.

The C version generates a height field and color map and provides keyboard-controlled movement. It was written as a simpler explanation of the earlier `newvox4` experiment.
