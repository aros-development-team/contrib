# MicroEMACS

MicroEMACS is a compact text editor included in AROS contrib.

It provides buffers, multiple windows, file editing, search, kill/yank operations and keyboard macros using Emacs-style key bindings.

See `me.doc` for the command reference.
