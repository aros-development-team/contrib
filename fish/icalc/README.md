# ICalc

ICalc is an interactive complex-number calculator included in AROS contrib.

Its expression parser supports arithmetic, exponentiation, complex conjugation, variables, constants and built-in functions.
