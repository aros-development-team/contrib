# grep

This directory contains a classic DECUS-derived `grep` implementation included in AROS contrib.

It searches files for text patterns and supports options for match counts, file names, line numbers and inverted matches.
