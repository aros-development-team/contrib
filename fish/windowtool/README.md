# WindowTool

WindowTool 1.0 is a Commodity for controlling windows with configurable keyboard shortcuts.

It can switch between windows, close or resize them, open a new Shell and expose additional window-management actions.

See `windowtool.doc` for configuration and hotkeys.
