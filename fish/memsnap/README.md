# MemSnapII

MemSnapII is a graphical memory monitor and snapshot utility.

It displays current, snapshot and used memory values for chip, fast and total memory, making it useful for observing memory use before and after running programs.

See `memsnap.doc` for configuration and usage.
