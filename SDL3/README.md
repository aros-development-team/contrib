# SDL 3

This directory contains the SDL 3 port for AROS together with selected companion libraries.

`main` provides the core SDL 3 port and AROS integration. `SDL3_image` and `SDL3_ttf` provide the corresponding add-on libraries.
