# MAD

This directory contains the MAD MPEG audio decoder package used by AROS contrib.

It builds the `madahi` and `madoss` players and a static `libmad` library. `madahi` uses AHI for audio output, while `madoss` uses the OSS-compatible audio interface.
