# SoX

This directory contains the AROS patch for SoX 12.17.4, the command-line sound processing utility.

Only the AROS patch is stored here; this directory does not contain the upstream source tree or an AROS contrib build recipe.
