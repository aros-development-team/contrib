# TinySID

TinySID 0.96 is a command-line SID music player for AROS.

Run it with a SID file as its argument. TinySID loads the file, displays its title, author and copyright information, and plays it until Return is pressed.

```text
TinySID <sid-file>
```
