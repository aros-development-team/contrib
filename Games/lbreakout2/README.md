# LBreakout2

This directory contains the AROS port recipe for LBreakout2 2.6.5, a Breakout-style arcade game.

The contrib build fetches the upstream sources, applies the included AROS patch, and builds the game with SDL-based dependencies.
