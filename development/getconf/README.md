# getconf

`getconf` is a command-line utility for querying system and path configuration values and POSIX limits.

The source is derived from NetBSD and the contrib build installs the tool in the AROS developer environment.
