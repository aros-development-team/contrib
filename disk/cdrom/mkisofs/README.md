# mkisofs

This directory contains the AROS port recipe for mkisofs 1.11.3, a command-line ISO 9660 image creation tool.

The contrib build fetches the upstream sources, applies the included AROS patch, and installs the resulting tool in the AROS developer environment.
