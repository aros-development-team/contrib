# OpenSSH

This directory contains the AROS port recipe for OpenSSH Portable 7.3p1.

The contrib build fetches the upstream OpenSSH sources and applies the included AROS patch before building the package.
