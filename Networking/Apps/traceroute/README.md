# traceroute

`traceroute` is a network diagnostic utility that displays the route packets take to a remote host and reports timing information for the hops along the way.

See `traceroute.guide` for usage and options.
