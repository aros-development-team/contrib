# NTPSync

NTPSync is an NTP time-synchronization utility for AROS.

It can query an NTP server and use the result to update the local system time, with configurable server, timeout and UTC offset settings.
