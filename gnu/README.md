# GNU and Unix ports

This directory collects AROS build recipes, patches and selected source trees for GNU and related Unix tools and libraries included in contrib.

Most package directories fetch upstream sources during the build and apply AROS-specific patches; some carry source directly. See each package's `mmakefile.src` and any package-specific documentation for details.
