# Unarc

Unarc is a MUI archive extraction utility for AROS using `xadmaster.library`.

It can be started from the CLI or Workbench. The archive and destination can be supplied from the command line or through icon tooltypes; a public screen can also be selected from the CLI.
