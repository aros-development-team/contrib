# chunkyiclass

`chunkyiclass` is a BOOPSI image class for displaying chunky image data.

It supports palettes and shared pens, selected-state imagery and bitmap scaling. The contrib build provides the link library together with a small test program.
