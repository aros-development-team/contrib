# ModifyIcon

ModifyIcon is a command-line tool for inspecting and modifying Amiga icon files.

It can change icon metadata and image data and convert between several icon representations.

The source notes that some handling relies on observed, undocumented icon structures, so modified files should be checked before relying on them.
