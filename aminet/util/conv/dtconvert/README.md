# DTConvert

DTConvert is a DataTypes-based file conversion utility.

It converts between compatible picture, animation, movie and sound formats using installed datatype classes and can be used from the command line or its GUI.

See `DTConvert.doc` for details.
