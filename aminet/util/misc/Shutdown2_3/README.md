# Shutdown

This directory contains the Shutdown package, including `shutdown.library`, `ShutdownSetup` and an example client.

The package tracks file and device activity and allows applications to register shutdown information so active work can be reported before restart or power-off.

Detailed original documentation is in `Shutdown/Shutdown.dok`.
