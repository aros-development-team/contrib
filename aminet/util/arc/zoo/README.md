# Zoo

This directory contains the source tree for the Zoo archive utility.

Zoo supports archive operations including adding, extracting, testing and listing files. The AROS MetaMake file is present, but its `%build_prog` rule is currently commented out.
