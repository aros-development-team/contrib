# LHa

LHa is an archive utility included in the Aminet collection in AROS contrib.

It provides the classic LHa/LZH archive operations through the `lha` command. Historical upstream notes are kept under `doc/`.
