# SmartTrash

SmartTrash 2.3 is a Workbench AppIcon trashcan installed by AROS contrib in `WBStartup`.

Dropped files are moved to a configurable trash directory, creating it when necessary. The AppIcon, menu item and trash path can be configured through tooltypes.
