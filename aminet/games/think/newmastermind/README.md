# NewMasterMind

NewMasterMind 1.2 is an implementation of the MasterMind code-breaking game.

The AROS build includes localized game logic and uses `chunkyiclass` for its graphics. See `newmastermind.guide` for gameplay and options.
