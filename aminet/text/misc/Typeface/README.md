# Typeface

Typeface 1.24 is an editor for Amiga bitmap fonts.

It can edit bitmap-font characters and parameters and can load outline fonts for conversion to bitmap fonts. The AROS build uses BGUI. See `docs/Typeface.guide` for usage.
