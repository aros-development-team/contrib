# SDL 2

This directory contains the SDL 2 port for AROS together with selected companion libraries.

`main` provides the core SDL 2 port and AROS integration. `SDL2_image`, `SDL2_mixer` and `SDL2_ttf` provide the corresponding add-on libraries.
